//var anuncio = alert ("Hola muy buenas") // Esto va a mostrar en la pagina "Hola muy buenas"

typeof dogId === "string" // true

/* if (condicion) {
    //entre llaves este codigo
    //se ejecuta si la condicion es true
} */

/* const edad = 20

if(edad >= 18) {
    console.log("Es mayor de edad")
} else if (edad === 0) {
    console.log("Recien nacido")
}else {
    console.log("es menor de edad")
}  */

/* while (condition) { // cuando la condition sea false no se ejecutara el codigo, si es true se ejecuta lo que haya dentro del codigo
    //console.log
} */

let cuentaAtras = 10

while (cuentaAtras > 0) {
    cuentaAtras = cuentaAtras -1
    console.log(cuentaAtras + ' segundos restantes')
}

const sum = (a,b)
